// FlukeFeatures — feature modules.
// Each subfolder (Sightings/, Whales/, etc.) is one feature.
// Features depend on FlukeKit + FlukeUI; never on each other.

public enum FlukeFeaturesVersion {
    public static let current = "0.1.0"
}
