// FlukeKit — pure Swift domain layer for the Fluke iOS app.
// No SwiftUI imports. No view code. API client, models, persistence,
// services live here.

public enum FlukeKitVersion {
    public static let current = "0.1.0"
}
