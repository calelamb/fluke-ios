// FlukeUI — design system for the Fluke iOS app.
// SwiftUI components, design tokens, shapes. No domain knowledge.

import SwiftUI

public enum FlukeUIVersion {
    public static let current = "0.1.0"
}
